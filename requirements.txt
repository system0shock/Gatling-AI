# Runtime Python dependencies for the Gatling-AI tools.
# Pinned to the tested environment for reproducibility (mirrors the pinned
# Java/Gatling versions in tools/gatling_generator/templates/pom.xml).
#
# Requires Python >= 3.11 (tools use datetime.UTC and PEP 604 unions).
#
# Install:  pip install -r requirements.txt
PyYAML==6.0.3
jsonschema==4.26.0
